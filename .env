# Client-side environment variables
# Note: API key is now handled server-side for security

# Database URL for authentication (required for production)
# For Neon database: DATABASE_URL=postgresql://username:password@hostname/database?sslmode=require
# For local SQLite: DATABASE_URL=file:./dev.db
# Leave commented out for testing (will use in-memory storage)
# DATABASE_URL=postgresql://username:password@hostname/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_a0cXyUDmAvp4@ep-sweet-water-agp4jmaq-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require